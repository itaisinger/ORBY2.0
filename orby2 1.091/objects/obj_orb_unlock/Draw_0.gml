image_index = index;

//face the orb
dir = approach(dir, 0.15, sign(obj_orb.x-x));
image_xscale = dir*scale;
image_yscale = scale

if(state != UNLOCK_STATES.off)
{
	sprite_index = spr_unlock;
	draw_self();
	var final_a = 0
	if(dir < 0)
	{
		final_a = image_angle - 180;
	}
}